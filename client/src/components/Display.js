import React from 'react';

const Card = (props) => {
  return (
   
      
        <h5>{props.user.name} - {props.user._id} - {props.user.date}</h5>
        
       
       
    
   
  )
}
export default Card;